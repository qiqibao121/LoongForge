# 当前验证进度（2026-08-28）

## FastWAM

- 远端目录：`/raid0/fastwam/experiments/fastwam_powersgd_plus_freeze_v_fair_a800_100_1000_v1`
- 两个 0–100 warmup 均已成功，并生成各自的 step100 DCP checkpoint。
- dense 与 `powersgd_plus_freeze_v_rank1` 主段均已完成 step1000；step301–1000 对齐 MAE=0.0006683、MAPE=0.0485%，最大绝对误差=0.0098457（step422）。冻结v平均 step time=1.0731 s、吞吐=0.9428，dense分别为0.8321 s和1.2114。
- FastWAM 运行期间未观察到 OOM、NaN、CUDA/NCCL 错误；`nan_iterations=0`、`skipped_iterations=0`。

## Cosmos3

- 远端目录：`/raid0/fastwam/experiments/cosmos3_powersgd_plus_freeze_v_fair_a800_100_1000_v1`
- FastWAM 完成后已启动，但 dense warmup 在模型从 meta tensor 迁移到 GPU 时失败；错误为 `Cannot copy out of meta tensor; no data!`。已分类为 Cosmos3 初始化/框架兼容性问题，不是 OOM，8 卡已释放，未计入有效对比。
- 已同步带“前序失败则阻断”的保护逻辑；只有确认实际 rank=1 压缩和冻结 `v` 激活后，才计入有效结果。

### 修复后重跑（v3）

- 远端目录：`/raid0/fastwam/experiments/cosmos3_powersgd_plus_freeze_v_fair_a800_100_1000_v3`
- 已完成两项最小兼容修复：在 DDP 包装前物化 meta tensor（原文件保留远端 `.bak_before_cosmos_meta_fix_20260828`），并将不支持的 `opencv` 视频后端切换为已安装的 `pyav`。
- 当前 dense warmup 正常占用 8 卡，最新观测 step 77/100；无 NaN/OOM/CUDA/NCCL 错误。随后将串行执行 freeze-v warmup、dense 主段和 PowerSGD+ freeze-v 主段。

## 归档约定

`manifest.csv` 记录每个实验的远端原路径和状态，`remote_snapshot/` 保存不含 checkpoint 的文本证据，`integrated_summary.csv/md` 保存跨模型汇总。活动实验结束后再将 `running/queued` 更新为最终状态和数值。

## 最终补跑（2026-09-01）

- Cosmos3 纯 PowerSGD+：`/raid0/fastwam/experiments/cosmos3_powersgd_plus_fair_a800_100_1000_v1`。
- `warmup_dense`、`warmup_powersgd_plus`、`dense`、`powersgd_plus_rank1` 均成功完成到 step1000；无 NaN、OOM、CUDA/NCCL 错误。
- step301–1000 对齐结果：MAE=0.01570647，MAPE=120.2158%，最大绝对误差=0.18225979（step482）；dense 平均 step_time=2.0023 s、吞吐=0.5048，纯 PowerSGD+ 平均 step_time=2.2613 s、吞吐=0.4558。
- rank=1 压缩在全局 step300（局部 iter199）启动，50 步周期重启共15次；显存峰值未写入 metrics，实验结束后 8 卡已释放。
